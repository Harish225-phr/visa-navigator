{
  "buildCommand": "npm run build && node scripts/generate-index.js",
  "outputDirectory": "dist/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}